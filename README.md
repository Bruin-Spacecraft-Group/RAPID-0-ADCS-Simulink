# RAPID-0-ADCS-Simulink

## Running the Project
- Clone the repository
- Open the project (double click the .prj file which runs "initialize.m")
- Run the model (double click the .slx file in the "models" folder and click "Run")